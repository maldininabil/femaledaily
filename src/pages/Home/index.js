import { Fragment, Helmet } from "libraries";

const Home = () => {
    return(
        <Fragment>
            <Helmet>
                <title>Homepage</title>
            </Helmet>
        </Fragment>
    )
}

export default Home;